# Anour Landing Page

This repository contains a simple static landing page for **anour**, a minimalist skincare brand. Open `index.html` in a browser to preview the design.

The page is intentionally lightweight and uses placeholder media in the `media/` directory. Replace these with your own high‑resolution assets for production use.
